# version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage

  # backend:
  #   build: ./backend
  #   ports:
  #     - "8000:8000"
  #   depends_on:
  #     - qdrant

  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "5173:5173"

volumes:
  qdrant_data:
